{
  "name": "procozone",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch src/public/data/db.json --port 3001 --routes src/public/data/routes.json",
    "start": "concurrently \"npm run dev\" \"npm run api\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4",
    "vite": "^5.4.0"
  }
}
